{
  "name": "my-own-dictionary",
  "version": "1.0.0",
  "description": "A personal dictionary app with mnemonics",
  "scripts": {
    "build": "echo 'No build step needed for static files'"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
